# 生産管理プランニング3級 学習アプリ — 開発仕様書（Claude Code用）v2

このファイルをリポジトリ直下に置き、`claude` に「SPEC.md を読んで実装して」と渡す前提の仕様書。
最優先ゴール：**スマホのスキマ時間での学習効率を最大化する**こと。

## 目的
2026年10月4日のビジネス・キャリア検定「生産管理プランニング3級」合格。
本番と同じ4択40問形式を、苦手優先で高速反復できることを最優先とする。
合格ライン＝40問中24問(60%)。

## 技術スタック（既存プロジェクトに合わせる）
- React 19 + TypeScript + Vite
- Dexie（IndexedDB）で問題データと学習履歴を端末ローカル保存
- vite-plugin-pwa でPWA化（ホーム画面追加・オフライン動作・端末ローカル通知）
- デプロイ先：GitHub Pages（`vite.config.ts` の `base` をリポジトリ名に設定）
- 外部通信なし。すべて端末内で完結（電波なしで使える）

## 問題データの二本立て
### A. 本番演習プール（公式過去問）＝ ユーザーが取り込む
- 公式過去問は著作物。アプリ側は「取り込む箱」として設計する。
- 取り込み手段：`origin: "official"` の Question をユーザーがJSON/CSVでインポートできる
  画面を用意（ファイル選択→バリデーション→Dexieへ保存）。
- CSVテンプレート（列: id, category, subcategory, stem, choice1..4, answerIndex, explanation, source）
  も同梱し、会社PCの過去問をコピペで流し込めるようにする。
### B. 日常反復プール（オリジナル練習問題）＝ 同梱シード
- `src/data/questions.seed.json`（`origin: "original"`）を初回起動時にDexieへ取込。
- 試験範囲の各テーマを網羅。随時追記していく。

## データモデル
```ts
interface Question {
  id: string;                 // "OFF-R06A-0001" / "ORIG-0001" など
  origin: "official" | "original";
  category: Category;
  subcategory?: string;
  stem: string;
  choices: [string, string, string, string];
  answerIndex: 0 | 1 | 2 | 3;
  explanation: string;
  source?: string;            // 出典メモ（自分用）
}

type Category =
  | "共通_品質管理" | "共通_原価管理" | "共通_納期管理"
  | "共通_安全衛生管理" | "共通_環境管理"
  | "PLN_生産システム設計" | "PLN_工場計画レイアウト"
  | "PLN_生産方式" | "PLN_製品開発設計"
  | "PLN_需要予測生産計画" | "PLN_在庫資材管理"
  | "PLN_購買外注管理" | "PLN_IE作業研究"
  | "PLN_設備管理保全";

interface StudyRecord {
  questionId: string;
  box: 1 | 2 | 3 | 4 | 5;     // Leitner箱。1=苦手, 5=定着
  dueAt: number;               // 次に出す予定時刻 epoch ms
  lastAnswered: number;
  correctCount: number;
  wrongCount: number;
}
```

## ★スマホ学習効率を最大化する設計（最重要）
学習科学（アクティブリコール／分散学習／インターリービング／即時フィードバック）を
UXに落とし込む。以下は必須要件。

1. **ゼロ摩擦起動**：アプリを開いたら即「次の1問」を表示（ホームを経由しない設定を既定に）。
   目標は起動→出題まで体感1秒以内。
2. **親指ゾーンUI**：問題文は画面上部、4つの選択肢は画面下半分に大きなボタンで配置。
   片手・親指だけで完結。誤タップ防止に選択肢間の余白を十分に取る。
3. **テンポ最優先ループ**：選択肢タップ→即採点（正解=緑/不正解=赤）→解説を同画面に展開
   →「次へ」1タップ（採点後はスワイプでも次へ可、採点前スワイプは無効化して誤爆防止）。
4. **SRSで“今忘れかけ”だけ出す**（下記ロジック）。1分の学習で最大の定着を狙う。
5. **インターリービング既定ON**：カテゴリを横断してシャッフル出題（分野を固めない）。
   任意でカテゴリ絞り込みも可能。
6. **マイクロセッション**：ホームに「5問」「10問」「3分」のワンタップ開始。
   短時間で区切れることを最優先（信号待ち・エレベーター・改札待ちで回せる）。
7. **中断に極端に強い**：1問回答ごとに即保存。途中で閉じても次回は続きから。
8. **オフライン完全動作**：PWAで電波なし（地下鉄等）でも全機能。
9. **習慣化の最小ゲーミフィケーション**：連続学習日数（ストリーク）と1日の目標問題数
   （既定20問）。達成でさりげない表示。過剰な演出はしない。
10. **間違いだけモード**：box1・2の問題だけを集中反復するショートカット。
11. **ダークモード**：夜のスキマ学習用に自動切替。
12. **毎日リマインド**：端末ローカル通知で1日1回（時刻設定可）。
    ※iOSのPWA通知は制約があるため、未対応環境ではアプリ内バナーにフォールバック。
13. **確信度入力は入れない**（摩擦になるため）。判定はタップ結果のみで自動。

## SRS（間違え優先再出題）ロジック
Leitner方式：
- 正解→ box +1（最大5）、`dueAt = now + 間隔[box]`
- 不正解→ box を 1 に戻し、`dueAt = now`（当日中に再登場）
- 間隔の目安：box1=即日, box2=1日, box3=3日, box4=7日, box5=14日
- 出題キュー：`dueAt <= now` を優先。その中で box 小さい順→`dueAt`古い順。
- キューが尽きたら未学習問題を投入。

## 画面（5つ）
1. **ホーム**：ワンタップ開始（5/10/3分/間違いだけ/本番）、今日の進捗、ストリーク、
   カテゴリ別正答率サマリ。
2. **一問一答**：SRSキューから1問ずつ（上記テンポループ）。
3. **本番シミュレーション**：`origin:"official"` 優先で40問ランダム・制限時間・カウントダウン。
   終了後 ◯/40 と合否（24で判定）＋カテゴリ別内訳。
4. **成績ダッシュボード**：カテゴリ別正答率の棒グラフ、box分布、連続学習日数、
   合格ライン到達予測（現状正答率ベースの簡易表示）。
5. **問題インポート**：CSV/JSON取込（公式過去問用）、バリデーション（id重複・answerIndex範囲）。

## 実装順序（Claude Codeへの指示）
1. Vite+React+TS+PWA初期化、GitHub Pages用base設定、ダークモード
2. Dexieスキーマ（questions, studyRecords）＋シード取込
3. **一問一答モード（最優先。ここだけで学習が回る）** ＋テンポループUX
4. SRSロジック
5. ホームのワンタップ開始＆ストリーク
6. 本番シミュレーション
7. インポート画面（公式過去問取込）＋CSVテンプレ
8. 成績ダッシュボード（recharts等の軽量ライブラリ）
9. PWAアイコン・manifest・ローカル通知・オフライン確認

## 非機能
- 起動〜1問目表示まで体感1秒以内
- 1問ごと即保存（中断耐性）
- 誤操作防止：採点前のスワイプ無効、回答確定は明示タップ

---

## 公式過去問の取り込みワークフロー（追補）

### 入手元（JAVADA公式・個人学習用）
JAVADAは各回1回分を「過去の問題例及び解答」として公開している。各回のインデックスから
「生産管理プランニング3級」の問題PDFと、3級一括の解答PDFを落とす。
- 各回インデックス：
  - 令和7年度後期 sample_test_r07l.html （プランニング3級問題＝r07l041b01x.pdf / 解答＝r07l_seikaihyou3kyu.pdf）
  - 令和7年度前期 sample_test_r07e.html （解答＝r07e_seikaihyou3kyu.pdf）
  - 令和6年度後期 sample_test_r06l.html （解答＝r06l_seikaihyou3kyu.pdf）
  - 令和6年度前期 sample_test_r06e.html （解答＝r06e_seikaihyou3kyu.pdf）
  - 令和5年度後期 sample_test_r05l.html （解答＝r05l_seikaihyou3kyu.pdf）
  ※問題PDFのファイル名は回により表記ゆれがあるため、各インデックスページのリンクから取得する。
  これで概ね5回分・約200問の公式問題が集まる。

### PDFの性質
問題PDFはテキスト埋め込み型（born-digital）。OCR不要でテキスト解析できる。
書式は「問題N」→「ア．イ．ウ．エ．」。各ページに「禁転載複製」透かしとページ識別子（例 R7後-041B01-3）。

### 変換
同梱の `tools/convert_javada_pdf.py`（pdfplumber使用）を各自のPCで実行し、
`questions.R0xx.json`（origin:"official"）を生成する。正解は解答PDFを見てCSVで渡す方式が最も確実。
計算式・穴埋め表・ルビを含む問題は自動抽出が乱れるので、出力を目視修正する。

### ★著作権上の取り扱い（必須）
公式問題は「禁転載複製」。個人学習の範囲でのみ使用し、**公開コードに埋め込まない**こと。
- `questions.official*.json` は **.gitignore** に追加（またはリポジトリを private に）。
- 公式問題はアプリの「インポート画面」から実行時に読み込み、**端末のIndexedDBにのみ保存**する。
  ＝公開されるコードには私が作成したオリジナル問題(`questions.seed.json`)だけが含まれる。
- 本番シミュレーションは、端末に取り込まれた official プールがあればそれを優先使用する。

---

## 間違い復習・解説・本番採点の詳細仕様（追補）

### ① 間違えた問題から学ぶ仕組み
- **即時フィードバック**：練習モードでは回答直後に「正解／不正解」を色で表示し、
  正解の選択肢をハイライト＋`explanation` を同画面に展開する。
- **誤答ログ**：不正解は `StudyRecord` に記録（wrongCount++、box=1、dueAt=now）。
  加えて「いつ・どの選択肢を選んで間違えたか」を保持し、復習画面で振り返れる。
- **復習（間違いだけ）モード**：box1・2、または直近の誤答だけを抽出して集中反復。
  正解できたら box が上がり一覧から外れる。
- **自分メモ**：問題ごとに自由記述メモ（誤答ノート）を保存できる。
- **解説の補完（重要）**：公式過去問は解説が付属しないため、次のいずれかで補う。
  (a) かんぽう過去問集の解説を `explanation` に手入力、
  (b) アプリ内「AI解説」ボタンでその場生成。
  - Claude.ai上のプレビュー(Artifact)では埋め込みAPIで無料生成できる。
  - GitHub Pages公開版では各自のAnthropic APIキーを設定画面から登録し（端末ローカル保存・
    非コミット）、オンデマンドで解説を生成する。キー未設定時はボタンを隠す。

### ② 本番形式＋採点
- **出題**：`origin:"official"` を優先して40問ランダム（不足分はoriginalで補完可）。
- **本番同様の挙動**：試験中は正誤を一切表示しない（提出後にまとめて採点）。
- **制限時間**：既定は要確認だが3級の標準に合わせて設定（暫定110分）。残り時間カウントダウン、
  0で自動提出。設定で変更可。
- **採点**：1問2.5点×40＝100点満点、合格ライン60点（24問）。提出後に
  得点／合否（合格・不合格を明示）／所要時間／カテゴリ別正答率を表示。
- **見直し**：採点後、誤答のみを正解＋解説付きで通し復習。これらは自動的に box1 へ。
- **受験履歴**：模試の結果（日時・得点・合否・所要時間）を保存し、**得点推移グラフ**で
  伸びを可視化。合格ライン(60点)を基準線として表示。
